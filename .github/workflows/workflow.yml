name: BWM-XMD Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.19.2 # Matches your package.json engine

      - name: 🧹 Clear npm cache
        run: npm cache clean --force

      - name: 📥 Install dependencies
        run: npm install

      - name: 🚀 Start bot
        run: npm start