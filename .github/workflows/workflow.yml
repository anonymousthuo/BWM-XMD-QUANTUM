name: BWM-XMD Bot CI

on:
  push:
    branches: [main] # or "master" if your repo uses that
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Use the version your bot needs

      - name: 📦 Install dependencies
        run: npm install

      - name: ▶️ Start Bot
        run: npm start